# 小说阅读器项目进度与安全升级记录

本项目是一个基于 Flask 开发的私人小说阅读器。本文档记录了本项目的核心功能实现、近期完成的安全升级以及服务器部署的进展。

---

## 1. 核心功能进展

- [x] **多源小说搜索**：自动发现并集成多个线上书源模块（已支持 `dadehe`、`biqu22`、`mayiwsk` 蚂蚁文学、`shuyuanks` 书源阁、`jkwxw` 吉克文学 等）。
- [x] **个人书架**：支持小说添加至书架，自动记录并持久化保存阅读进度。
- [x] **极致阅读体验**：
  - 现代化深色极简视觉系统。
  - 支持字体大小、行高、段落缩进等自定义阅读设置。
  - 集成 Playwright 自动渲染技术，应对高级防爬与 Cloudflare 挑战页面。
  - 支持后台章节预加载，阅读翻页无感切换。

---

## 2. 安全升级（已完成）

针对“仅限个人使用”的需求，项目于 **2026-05-24** 进行了全面的安全加固：

- [x] **Session 密码登录校验**：
  - 为所有主页面和 API 接口增加了认证装饰器。
  - 未登录用户访问任何路由均会强制重定向至精美的自定义登录页验证身份。
- [x] **敏感信息外置 (.env)**：
  - `SECRET_KEY` 和 `PASSWORD_HASH` 从源码中剥离，移入本地环境变量 `.env`。
  - 更新了 `.gitignore`，确保密钥与密码哈希绝不上传至 GitHub。
- [x] **新增密码设置工具**：
  - 新增了 `setup_password.py` 工具，支持在终端交互式生成高强度加密的密码哈希值并自动写入配置文件。
- [x] **Session 安全加固**：
  - 开启 `HttpOnly` 防止 XSS 攻击读取 Cookie。
  - 配置 `SameSite="Lax"` 抵御 CSRF 攻击。
- [x] **默认监听收紧**：
  - 默认仅绑定本地 `127.0.0.1` 接口，仅允许本地访问。在需要暴露给外网或局域网时通过 `HOST=0.0.0.0` 显式开启。

---

## 3. 服务器部署进展 (Azure VM)

- [x] **代码同步**：在本地开发完成后，通过 GitHub 远程仓库一键同步至服务器。
- [x] **服务托管**：在服务器上配置了 `systemd` 系统服务，实现了 `novel-reader` 服务的开机自启、故障恢复与标准日志收集。
- [x] **配置文档维护**：编写并提交了 [server_debugging_guide.md](server_debugging_guide.md) 指南，方便在服务器端进行服务控制、日志追踪和环境修复。
- [x] **外网访问适配**（进行中）：
  - 修改 `novel-reader.service` 环境变量使服务监听 `0.0.0.0`。
  - 在 Azure 控制台安全组开放 `5000` 端口以支持手机等移动端访问。
